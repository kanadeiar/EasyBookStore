@model CatalogWebModel
@{
    ViewBag.Title = "Каталог товаров";
}

<div class="container-md">

    <div class="row">
        <div class="col-sm-3">
            <partial name="Partial/_LeftSidebarPartial" />
        </div>
        <div class="col-sm-9">
            <partial name="Partial/_ProductsPartial" model="Model.Products"/>
        </div>
    </div>

</div>

